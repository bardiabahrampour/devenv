#ifndef PROJECT_H
#define PROJECT_H
#include <vector>
#include <string>
//define project properties and values

struct Project{
    std::string name;
    std::vector<std::string> files,blacklist;
};

#endif //PROJECT_H